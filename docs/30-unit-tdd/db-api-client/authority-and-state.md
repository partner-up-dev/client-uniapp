# Authority and state

- Base URL is derived from VITE_PGRST_URL
- Table name and schema are stored per client instance
- Auth headers come from src/store/account via getAuthHeaders
- Table schema is stored and normalized for Valibot class support
